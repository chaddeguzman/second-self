# Synthetic backup/restore drill fixture

This directory intentionally contains only public synthetic test data. Use it to
exercise archive-policy and restore behavior; never substitute a personal vault.
