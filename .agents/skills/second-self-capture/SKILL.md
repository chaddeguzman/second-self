---
name: second-self-capture
description: Capture a thought, note, quote, link, or live user correction into the Second Self inbox. Use when the user asks to remember, record, save, capture, or reconcile new personal context.
---

# Second Self Capture

Version: `1.0.0`

1. Preserve the user's wording and distinguish it from agent interpretation.
2. Run `python -m second_self capture --title "<title>" --body "<body>"`.
3. Add explicit source and project links.
4. Leave classification as `inbox`; weekly review determines durable placement.
5. Return the created private path without exposing its contents publicly.

